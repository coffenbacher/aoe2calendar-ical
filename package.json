{
  "scripts": {
    "start": "NODE_ENV=development nodemon index.js --exec babel-node",
    "generate": "NODE_ENV=development nodemon generate.js --exec babel-node"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "express": "^4.16.2",
    "google-spreadsheets": "^1.0.0",
    "ics": "^2.4.1"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "nodemon": "^1.17.1"
  }
}
